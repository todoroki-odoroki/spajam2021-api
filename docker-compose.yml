version: '3'
services:
  app:
    build:
      context: .
    # dependes_on:
    #   - db
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
    command: ["uvicorn", "app.main:app", "--reload", "--host", "0.0.0.0"]